create an agro init command which will create a directory + file structure in the root of the repo taking the following form:

.agdocs/
- specs/
- swap/
- conf/
- .gitignore: content is to "swap/"

Also add .agdocs to the repo root's .gitignore
